# TrendHunter — Mobile Web Prototype

TrendHunter помогает креаторам и селлерам находить трендовые товары для дома, просматривая встроенные посты из TikTok и Instagram, анализируя метрики вовлеченности и сохраняя перспективные товары в воронку "В работе".

## ✨ Возможности

### Лента (Feed)
- 📱 Вертикальный фид в стиле Stories (один пост на экран)
- 🎵 Встроенные посты из TikTok и Instagram
- 📊 Метрики вовлеченности (просмотры, лайки, комментарии)
- 🎯 Расчет насыщенности рынка
- 🔍 Фильтрация по категориям (Дом, Дети, Техника)
- ⚡ Сортировка по различным параметрам
- 💾 Сохранение товаров в "В работе"
- 🚀 Ленивая загрузка embed'ов (только видимые посты)

### Дашборд
- 📈 KPI: Всего товаров, Сохранено, Средняя насыщенность
- 📊 Топ-5 товаров с ростом за 7 дней (sparklines)
- 📋 Список сохраненных товаров

## 🛠 Технологии

- **Backend**: Node.js + Express
- **Frontend**: Vanilla JavaScript (без фреймворков)
- **Стили**: Tailwind CSS (через CDN)
- **Тестирование**: Node.js native test runner
- **Хранение**: localStorage (для сохраненных товаров)

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск тестов

```bash
npm test
```

Все 35 тестов должны пройти успешно:
- ✅ 5 тестов конфигурации
- ✅ 8 тестов дашборда
- ✅ 12 тестов логики
- ✅ 7 тестов embed монтирования
- ✅ 3 теста сервера

### Запуск сервера

```bash
npm start
```

Сервер запустится на `http://localhost:3000`

## 📁 Структура проекта

```
trendy-shop/
├── server.js              # Express сервер
├── package.json           # Зависимости и скрипты
├── public/                # Статические файлы
│   ├── feed.html         # Страница ленты
│   ├── dashboard.html    # Страница дашборда
│   ├── config.js         # Конфигурация (TEXT_CONFIG, POST_CONFIG)
│   └── app.js            # Основная логика приложения
└── tests/                 # Тесты
    ├── server.test.js    # Тесты сервера
    ├── config.test.js    # Тесты конфигурации
    ├── logic.test.js     # Тесты бизнес-логики
    ├── mount-window.test.js  # Тесты embed монтирования
    └── dashboard.test.js # Тесты дашборда
```

## 🔧 Конфигурация

### TEXT_CONFIG (public/config.js)

Все текстовые строки UI находятся в `TEXT_CONFIG`. Никаких хардкодных строк в коде нет.

### POST_CONFIG (public/config.js)

Содержит точные ссылки на посты TikTok и Instagram:
- 10 TikTok постов от @karissa.brighton
- 10 Instagram reels

Все URL нормализованы (удалены query параметры и фрагменты).

## 📊 Формула насыщенности

```javascript
saturationScore = views / (likes + comments)
```

- Меньше = лучше (больше вовлеченности на просмотр)
- Цветовая кодировка:
  - 🟢 < 50: Отлично
  - 🟡 50-100: Хорошо
  - 🟠 100-200: Средне
  - 🔴 > 200: Плохо

## 🎯 Особенности реализации

### Ленивая загрузка embed'ов

Используется `IntersectionObserver` для монтирования embed'ов:
- Загружаются только видимые посты ±1 слайд
- Embed'ы размонтируются при выходе из viewport
- Автоматический fallback при ошибке загрузки

### Персистентность данных

- Сохраненные товары хранятся в `localStorage`
- Состояние сохраняется между сессиями
- Автоматическая синхронизация при изменениях

### Фильтрация и сортировка

**Категории:**
- Все
- Дом
- Дети
- Техника

**Сортировка:**
- По насыщенности (лучше → хуже / хуже → лучше)
- По цене (дешевле → дороже / дороже → дешевле)
- По просмотрам (сначала больше)

## 🧪 Тестирование

### Запуск всех тестов

```bash
npm test
```

### Запуск отдельных тестов

```bash
node --test tests/server.test.js
node --test tests/config.test.js
node --test tests/logic.test.js
node --test tests/mount-window.test.js
node --test tests/dashboard.test.js
```

## 📱 Использование

1. Откройте `http://localhost:3000/feed` для просмотра ленты
2. Свайпайте вверх/вниз для навигации между постами
3. Используйте фильтры и сортировку для поиска нужных товаров
4. Нажмите "В работу" для сохранения товара
5. Перейдите на дашборд для просмотра статистики и сохраненных товаров

## ✅ Соответствие спецификации

- ✅ Vanilla JavaScript (без React/Vue/Svelte)
- ✅ Tailwind CSS через CDN
- ✅ Без build step
- ✅ Весь UI на русском языке
- ✅ TEXT_CONFIG со всеми строками
- ✅ POST_CONFIG с точными ссылками TikTok
- ✅ Нормализация URL
- ✅ Формула насыщенности
- ✅ Вертикальный фид (один пост на экран)
- ✅ Ленивая загрузка embed'ов
- ✅ IntersectionObserver для монтирования
- ✅ Фильтрация и сортировка
- ✅ Сохранение в localStorage
- ✅ Дашборд с KPI
- ✅ Sparklines для трендовых товаров
- ✅ Список сохраненных товаров
- ✅ Все 35 тестов проходят

## 🎨 UI/UX

- Адаптивный дизайн для мобильных устройств
- Темная тема для ленты (лучше для видео контента)
- Светлая тема для дашборда (лучше для аналитики)
- Плавные переходы и анимации
- Градиентные оверлеи для читаемости
- Цветовая кодировка метрик

## 🔒 Ограничения

- **Embed'ы Instagram** могут не работать из-за ограничений платформы (предусмотрен fallback)
- **Данные** генерируются на клиенте (в production используйте API)
- **История роста** генерируется рандомно (в production используйте реальные данные)

## 📝 Лицензия

MIT

---

**Разработано в соответствии с technical specification для Senior Frontend + Node.js Prototyping Engineer**
